# Allow us to easily switch to different Ember Data adapters

# `import Adapter from 'appkit/adapters/local-storage'`
# `import Adapter from 'appkit/adapters/fixture'`
`import Adapter from 'appkit/adapters/orchestrate-io'`

`export default Adapter`
